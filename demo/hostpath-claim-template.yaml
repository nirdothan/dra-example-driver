apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: hostpath-claim-template
  namespace: default
spec:
  spec:
    devices:
      requests:
      - name: hostpath
        exactly:
          deviceClassName: hostpath.network.com
          count: 1
