---
apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod-1
  namespace: default
spec:
  containers:
  - name: test
    image: ubuntu:22.04
    command: ["bash", "-c"]
    args:
    - |
      echo "Pod: hostpath-pod-1"
      echo "Network device: $KUBEVIRT_NETWORK_DEVICE"
      echo "Network path: $KUBEVIRT_NETWORK_PATH"
      echo "Directory contents:"
      ls -la $KUBEVIRT_NETWORK_PATH
      echo "Creating test file..."
      echo "Hello from hostpath-pod-1" > $KUBEVIRT_NETWORK_PATH/test-file.txt
      echo "File created. Sleeping..."
      sleep infinity
    resources:
      claims:
      - name: hostpath
  resourceClaims:
  - name: hostpath
    resourceClaimTemplateName: hostpath-claim-template
---
apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod-2
  namespace: default
spec:
  containers:
  - name: test
    image: ubuntu:22.04
    command: ["bash", "-c"]
    args:
    - |
      echo "Pod: hostpath-pod-2"
      echo "Network device: $KUBEVIRT_NETWORK_DEVICE"
      echo "Network path: $KUBEVIRT_NETWORK_PATH"
      echo "Directory contents:"
      ls -la $KUBEVIRT_NETWORK_PATH
      echo "Creating test file..."
      echo "Hello from hostpath-pod-2" > $KUBEVIRT_NETWORK_PATH/test-file.txt
      echo "File created. Sleeping..."
      sleep infinity
    resources:
      claims:
      - name: hostpath
  resourceClaims:
  - name: hostpath
    resourceClaimTemplateName: hostpath-claim-template
---
apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod-3
  namespace: default
spec:
  containers:
  - name: test
    image: ubuntu:22.04
    command: ["bash", "-c"]
    args:
    - |
      echo "Pod: hostpath-pod-3"
      echo "Network device: $KUBEVIRT_NETWORK_DEVICE"
      echo "Network path: $KUBEVIRT_NETWORK_PATH"
      echo "Directory contents:"
      ls -la $KUBEVIRT_NETWORK_PATH
      echo "Creating test file..."
      echo "Hello from hostpath-pod-3" > $KUBEVIRT_NETWORK_PATH/test-file.txt
      echo "File created. Sleeping..."
      sleep infinity
    resources:
      claims:
      - name: hostpath
  resourceClaims:
  - name: hostpath
    resourceClaimTemplateName: hostpath-claim-template
